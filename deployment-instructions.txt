db.games.find({}).forEach(function(game) {
    game.settings.specialGalaxy.randomWarpGates = game.settings.specialGalaxy.randomGates;
    game.settings.specialGalaxy.randomWormHoles = 'none';

    delete game.settings.specialGalaxy.randomGates;

    db.games.save(game);
});
