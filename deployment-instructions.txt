db.users.updateMany({}, {
    $set: {
        isEstablishedPlayer: false
    }
});

db.users.updateMany({
    $or: [
        {'achievements.rank': { $gt: 0 }},
        {'achievements.completed': { $gt: 0 }}
    ]
}, {
    $set: {
        isEstablishedPlayer: true
    }
});
