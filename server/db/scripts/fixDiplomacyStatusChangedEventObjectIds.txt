db.gameevents.find({
    type: 'playerDiplomacyStatusChanged',
    'data.playerIdTo': {
        $type: 'string'
    }
}).forEach(ge => {
    ge.data.playerIdTo = new ObjectId(ge.data.playerIdTo)

    db.gameevents.save(ge)
});
